import { TouchableOpacity, View } from "react-native"

export const SettingsScreen = ({}) => {
    return (
        <View>
            <TouchableOpacity onPress={() => {}}><Text>Log In</Text></TouchableOpacity>
        </View>
    )
}