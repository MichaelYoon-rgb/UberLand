import React from "react"
import { View } from "react-native"

export const AlertComponent = () => {
    return (
        <View style={{position: "absolute", width: "100%", height: "100%", backgroundColor: "red"}}></View>
    )
}